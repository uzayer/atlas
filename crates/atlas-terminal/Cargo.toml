[package]
name = "atlas-terminal"
version = "0.1.0"
edition = "2021"

[dependencies]
portable-pty = "0.8"
serde = { version = "1", features = ["derive"] }
tokio = { version = "1", features = ["full"] }
anyhow = "1"
uuid = { version = "1", features = ["v4"] }
