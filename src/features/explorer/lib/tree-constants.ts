// Shared sizing for any tree view (project file tree, knowledge tree,
// future trees). Kept here so the project and knowledge panels stay
// visually identical when tweaks land.
export const ROW_HEIGHT = 26;
export const INDENT_PER_LEVEL = 14;
